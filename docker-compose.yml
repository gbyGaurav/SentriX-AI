version: '3.8'

services:
  db:
    image: postgres:16-alpine
    container_name: sentrix-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: sentrix_user
      POSTGRES_PASSWORD: sentrix_password
      POSTGRES_DB: sentrix_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sentrix_user -d sentrix_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: sentrix-backend
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql+asyncpg://sentrix_user:sentrix_password@db:5432/sentrix_db
      DATABASE_URL_SYNC: postgresql://sentrix_user:sentrix_password@db:5432/sentrix_db
      ALLOWED_ORIGINS: http://localhost:3000,https://sentrix-ai.vercel.app
      LLM_PROVIDER: ${LLM_PROVIDER:-openai}
      LLM_API_KEY: ${LLM_API_KEY:-}
      LLM_MODEL: ${LLM_MODEL:-gpt-4o-mini}
      ENABLE_LLM: ${ENABLE_LLM:-true}
      ENABLE_VIDEO_PROCESSING: "true"
      ENABLE_AUDIO_PROCESSING: "false"
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: sentrix-frontend
    restart: unless-stopped
    depends_on:
      - backend
    environment:
      NEXT_PUBLIC_API_URL: http://backend:8000
    ports:
      - "3000:3000"

volumes:
  postgres_data:
